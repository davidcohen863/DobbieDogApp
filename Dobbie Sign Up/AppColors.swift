import SwiftUI

enum AppColors {
    static let background   = Color(.systemGroupedBackground)        // screen bg
    static let secondaryBg  = Color(.secondarySystemGroupedBackground)
    static let card         = Color(.secondarySystemBackground)       // tiles/cards
    static let mutedCard    = Color(.systemGray6)                     // subtle tiles
    static let separator    = Color(.separator)
}
